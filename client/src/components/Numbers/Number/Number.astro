---
import type { NumberData } from "@data/_types/types";

interface Props {
  numberData: NumberData;
}

const { numberData } = Astro.props;
---

<div class="col-lg-4 col-4">
  <div class="counter-card">
    <h2 class="text-center text-lg-start">
      <span class="target">{numberData.suffix}</span>
      <span class="odometer odometer-auto-theme" data-count={numberData.count}>
        <span class="odometer-inside">
          <span class="odometer-digit">
            <span class="odometer-digit-spacer">{numberData.count}</span>
            <span class="odometer-digit-inner">
              <span class="odometer-ribbon">
                <span class="odometer-ribbon-inner">
                  <span class="odometer-value">{numberData.count}</span>
                </span>
              </span>
            </span>
          </span>
          <span class="odometer-digit">
            <span class="odometer-digit-spacer">{numberData.count}</span>
            <span class="odometer-digit-inner">
              <span class="odometer-ribbon">
                <span class="odometer-ribbon-inner">
                  <span class="odometer-value">{numberData.count}</span>
                </span>
              </span>
            </span>
          </span>
        </span>
      </span>
    </h2>
    <p>{numberData.description}</p>
  </div>
</div>
